# Netlify redirects file

# Redirect domain aliases to primary domain
https://www.stiftelseguiden.se/* https://stiftelseguiden.se/:splat 301!

# Common misspelling of the llms.txt convention
/llm.txt /llms.txt 301

# Let static pages and Netlify 404 handling respond with correct status codes

# Redirect old URLs if needed
# /old-page    /new-page    301

# Networkr proxy delivery — /insikter served by the Networkr SSR via forced
# 200 rewrite (same URL, this domain), zero rebuilds.
/insikter/* https://api.networkr.dev/blog-page/stiftelseguiden-se/:splat 200!
/insikter https://api.networkr.dev/blog-page/stiftelseguiden-se 200!